apiVersion: v1
kind: Service
metadata:
  name: comin-organization-service
  namespace: comin-dev
spec:
  selector:
    app: comin-organization-service
  ports:
  - port: 80
    targetPort: 8081
  type: ClusterIP